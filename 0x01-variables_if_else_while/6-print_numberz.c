#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - How can it be
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int k;

	for (k = '0'; k <= '9'; k++)
		putchar(d);
	putchar('\n');

	return (0);
}
